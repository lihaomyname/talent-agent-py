## 1. 接入与快照设计

- [x] 1.1 用合成图片验证实际 deepseek-flash 网关图片/JSON 支持与错误响应，记录不含凭据的结果。
- [x] 1.2 确认 Java 经历、日期和分页契约；按业务约定不复核人选可见性，不默认存在全文检索或额外详情接口。
- [x] 1.3 定义预算、功能开关、上传/缓存容量及快照上限配置。
- [x] 1.4 定义复用现有 JSON 的 V2 plan/run/reply/draft 类型、中文字段说明、V1 读取兼容规则；确认不新增表或迁移。

## 2. 兼容读写与恢复基础

- [x] 2.1 实现双版本快照编解码及未知版本错误，先覆盖旧记录回归。
- [x] 2.2 适配计划、澄清仓储及 get/history/get_result/重复消息读取，避免新快照误用旧模型。
- [x] 2.3 复用消息幂等与 Run 关联，统一活动指针和计划版本序列，原子提交确认与启动。
- [x] 2.4 实现 checkpoint 短事务、JSON 重新赋值、批内缓冲和预算消耗预记，验证游标不越过未提交人选。
- [x] 2.5 实现启动标记中断、历史读取和用户主动继续；不自动恢复模型调用。

## 3. 输入与编辑

- [x] 3.1 实现统一需求及来源结构，区分固定筛选、证据偏好和歧义。
- [x] 3.2 实现文字提取、截图校验/识别，保存提取文字和草稿，不存原图。
- [x] 3.3 实现卡片和对话共用的显式增删改/分类调整；缺失保留，确认新版本才替代任务。
- [x] 3.4 实现纯筛选截图和跨模式结构化交接，不重复文字解析，不丢未修改条件。
- [x] 3.5 测试岗位/候选人信息混淆、专项工龄、图片注入、非职位图片与不清晰文字。

## 4. 资料与证据匹配

- [x] 4.1 实现专用资料检索类型，共用 Java HTTP/分页适配，保持 V1 卡片不变。
- [x] 4.2 清洗白名单资料、去重、限长、标记缺失，移除宽响应日志及联系方式别名泄露。
- [x] 4.3 实现证据提取、来源校验、有限修复与评估失败处理。
- [x] 4.4 实现固定条件召回、信息不足说明及确定性偏好排序，偏好不淘汰候选人。
- [x] 4.5 保存必要资料/证据快照，测试缺失经历仍可展示卡片、语义不等价及虚构引用拒绝。

## 5. 有界运行和继续

- [x] 5.1 扩展现有 TaskRegistry 协调两种模式，验证闲聊、停止、进度及跨模式替代。
- [x] 5.2 实现固定查询分批检索、去重、统一预算与各停止原因，不逐页创建计划或结果组。
- [x] 5.3 实现继承最新 checkpoint 的继续、新 Run 独立消耗和稳定结果组；拒绝旧祖先分叉。
- [x] 5.4 实现累计轮数/人数/快照大小上限及有界运行状态，任务清理不删除数据库记录。
- [x] 5.5 实现 V2 会话作用域，默认轮询元数据、历史资料直接读取快照、凭据仅协程暂存。
- [x] 5.6 测试预算耗尽、重复页、源耗尽、取消、部分批次崩溃、重启继续及重复请求。

## 6. 页面与交付

- [x] 6.1 实现需求、唯一确认启动、匹配读取、继续和取消接口，保持旧普通搜索兼容。
- [x] 6.2 实现截图确认编辑、单进度卡、证据汇总与按需求版本展示历史。
- [x] 6.3 实现关闭页面/刷新/重启后的数据库恢复，旧版本继续禁用，普通分页保持原位更新。
- [x] 6.4 执行 V1/V2 混合历史、权限、前端及重启恢复测试，确认无 DDL 变更或迁移。
- [x] 6.5 用合成及授权脱敏评测验证语义准确性与预算延迟，不允许越权、编造证据或自动放宽要求。
- [x] 6.6 更新部署/回滚说明：先双版本读取后开放新写入，关闭功能仍保留历史可读，不删除数据回滚。
