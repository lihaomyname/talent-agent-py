# 多模态接入验证

2026-09-17：使用当前已配置的 DeepSeek 官方端点和凭据，发送内存中生成的合成职位 PNG（Combat Designer、本科、UGC 优先），请求 deepseek-flash 与 json_object。

结果：HTTP 200，返回合法 JSON，正确区分职位、本科要求和 UGC 偏好。未发送真实候选资料，未记录凭据。

该结果验证正常图片输入与 JSON 模式；无效图片、错误响应和正式客户端尚需后续测试，不代表任务 1.1 已全部完成。视觉配置与文本配置分开，正式运行须显式配置视觉端点和密钥。

Java 字段依据已提供样本与现有适配器：教育/工作经历、页码接口已有结构；workYears 日期语义尚未通过实际契约验收，不作为数值年限使用。任务 1.2 保持未完成；历史访问规则见下文。

## 历史资料读取规则（已确认）

业务确认候选人不会删除且均可查看，不需要额外复核可见性。历史资料在校验会话所有者与租户后直接读取数据库；继续直接继承进度。已删除原先额外检索复核及其预算配置。功能开关仍默认关闭，真实数据准确性验收独立进行。

## 语义合成评测

2026-09-17 使用当前文本模型对两条合成资料执行同一要求“有枪械后坐力调优经历”：明确写有“负责枪械后坐力调优并完成实机测试”的样本得到 SUPPORTED；只写“搜打撤/MOBA、角色技能设计、未描述枪械调优”的样本得到 UNKNOWN。引用均通过来源字段校验，没有把相关题材或职位头衔当成直接证据。

自然语言“找战斗策划，必须有枪械后坐力调优经历，UGC经验优先”经实际模型输出后，正确生成职位筛选和证据偏好。当前候选资料主要来自教育经历和工作经历，因此除固定搜索字段外，语言、框架、项目经验及能力描述都只用于偏好排序和证据说明；资料未提及某项能力时记为 UNKNOWN，不淘汰候选人。首次验证暴露职位补丁曾返回字符串的问题，提示词增加字段结构和示例后复验通过。自动化另外覆盖虚构引用、缺失经历、图片格式/像素/体积、错误网关、预算、取消、重启和幂等。
